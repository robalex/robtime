<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.OpenApi" Version="10.0.10" />
    <PackageReference Include="Microsoft.EntityFrameworkCore" Version="10.0.10" />
    <PackageReference Include="Microsoft.OpenApi" Version="2.11.0" />
    <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" Version="10.0.3" />
    <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL.NodaTime" Version="10.0.3" />
    <PackageReference Include="NodaTime.Serialization.SystemTextJson" Version="1.4.0" />
  </ItemGroup>

  <ItemGroup>
    <!-- Only Persistence — this project does CRUD, not calculation. It gets TimeCalculation.Model
         transitively through Persistence. Add a reference to the TimeCalculation engine project
         only when an endpoint needs to actually invoke PayCalculator. -->
    <ProjectReference Include="..\TimeCalculation.Persistence\TimeCalculation.Persistence.csproj" />
  </ItemGroup>

</Project>
