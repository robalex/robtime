{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "PayrollDb": "Host=localhost;Port=5432;Database=robtime;Username=postgres;Password=postgres"
  },
  "Cognito": {
    "_comment": "Placeholder until Terraform provisions a real User Pool (blocked on AWS credentials -- see DEPLOY_PLAN.md §4). JwtBearer only fetches the JWKS document lazily on first token validation, so a placeholder Authority doesn't break a plain dotnet build/run -- it just means no request can authenticate yet.",
    "Authority": "https://cognito-idp.us-east-1.amazonaws.com/REPLACE_WITH_USER_POOL_ID",
    "UserPoolClientId": "REPLACE_WITH_APP_CLIENT_ID"
  }
}
